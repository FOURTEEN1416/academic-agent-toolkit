# Python
__pycache__/
*.py[cod]
*$py.class
# ⚠️ 例外（2026-09-19 修复）：科研工具箱/tools/ 下的 16 个 .pyc 是**受控分发件**，
# 与同名 .py 加载器配对（加载器由 tools/pyc_loader.py 生成，3.11 编译器锁定），
# 属本仓既有设计。上面 `*.py[cod]` 曾把它们全部排除 → 公开 clone 后 16 个工具
# 一律报 "xxx.pyc not found" 并 exit 1（CI 首次实测发现，属发布物残缺）。
!科研工具箱/tools/*.pyc
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Virtual environments
.venv/
.venv*/
venv/
ENV/
env/
.env
.env.*
!.env.example
**/.env
**/.env.*
# 修复（2026-09-19）：上一行的 `**/.env.*` 会连带吃掉子目录的模板文件，
# 而 `!.env.example` 只对仓库根生效 → 科研工具箱/.env.example（纯 <REPLACE_ME> 模板，
# 无真实密钥）被误忽略，导致公开 clone 后 test_security_baseline 缺件失败。
!**/.env.example

# IDEs
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
Thumbs.db
Desktop.ini

# Testing
.pytest_cache/
.coverage
htmlcov/
.mypy_cache/
.ruff_cache/
.tox/

# Logs
*.log
logs/

# Internal development truth docs (not for public delivery)
dev-docs/

# Engine state and audit trails (not for public delivery)
.engine/
**/.engine/

# WorkBuddy 项目工作数据（AI 工作记忆，含本机绝对路径与个人身份线索；本仓为 PUBLIC，
# 故排除。2026-09-19 建立时首次出现 .workbuddy/memory/）
.workbuddy/

# Database
*.db
*.sqlite
*.sqlite3

# Node
node_modules/
.pnpm-store/

# Third-party competition material and private references (no redistribution right)
参考论文/
参考论文使用指南.md
附件*.pdf
赛前试炼任务/
CUMCM2026Problems/
extracted_images/
screenshots/

# ⚠️ 2026-09-19 加入（原为游离未跟踪项）：内含第三方出品模板
# (`2026年数学建模竞赛模板-BZD数模社.docx` 等 11 文件/1.5MB)，无再分发授权，
# 与 CUMCM2026Problems/ 同类隔离。本地可正常使用；公开 clone 不含此目录，
# 故 engine/modex-core/templates.json 指向此处的资产指针在公开形态下失联
# ——已由 tests/test_asset_utilization.py 的"缺件即 skip"语义覆盖，
# 若将来获得再分发授权，移除此行即可恢复入库。
CUMCM论文模板/

# Real practice/validation workspaces
workspaces/

# ⚠️ 2026-09-19 用户裁定：公开基准集废弃入库（benchmarks/cumcm_public + six_domains_public）。
#   内容零丢失——77 文件/271KB 已归档至 dev-docs/archive/legacy-benchmarks-tests-20260919/（含恢复命令）。
#   随之废弃的唯一依赖测试：tests/test_cumcm_benchmark.py（另两个根级门禁测试已保留入库）。
benchmarks/

# ⚠️ 永久丢失留痕：benchmarks/cumcm_private/（私有基准）从未入库，仅存于磁盘；
#   2026-09-19 扫描确认该目录已随 benchmarks/ 整体从磁盘消失，git 历史中无任何对象可恢复。

# Legacy toolkit and release packages (kept locally for traceability)
releases/

# Browser automation state and acceptance-test scratch artifacts
.playwright-mcp/
acceptance_testing/

# Suite-internal private reference data and anti-AI baselines derived from award papers
# ⚠️ 2026-09-19 修正：原为整目录 `科研工具箱/data/`。整目录规则有静默陷阱——
#    已 tracked 的老文件**免疫 ignore**（照常入库），但新增的交付件会**无声消失**
#    （git status 里根本不出现），导致本地全绿、公开 clone 缺件、对应测试全红。
#    故改为"忽略目录内容 + 显式放行交付件"。
#    ⚠️ negation 对被忽略的**目录**无效，必须写 `data/*` 而非 `data/`，否则 ! 规则全部失效。
#    （data/ 下 8 个历史文件已于 2026-08-28 入库，靠 tracked 免疫延续，不在此列）
科研工具箱/data/*
!科研工具箱/data/skill_tiers.json
!科研工具箱/data/skill_routing_index.json
!科研工具箱/data/skill_routing_critical.json
!科研工具箱/data/contest_lessons.json
!科研工具箱/data/contest_lessons.md
科研工具箱/baseline/

# Local test and vision-call residue
科研工具箱/_test_input.md
**/.drawio_vision_calls.json
**/.tikz_vision_calls.json
# Upstream fork staging (local research only, not tracked)
vendor/

# ZCode compat: NTFS junction to kit skills (not trackable)
.zcode/skills/
方法武器库/

# Modex v3 技能包原包（本地对照源，已定向吸收，赛后评估入库方式）
modex-3-skills/

# 无 License 上游技能（本地完整复刻可用，不入公开仓——侵权红线隔离）
科研工具箱/skills/plot-from-data/
科研工具箱/skills/plot-from-image/
科研工具箱/skills/visio-image-rebuilder/
科研工具箱/skills/paper-framework-figure-studio-pro/

# No-license upstream skill batch (2026-09-11): eco-community-plots
科研工具箱/skills/eco-community-plots/

# Third-party aesthetics/reference galleries and personal material (2026-09-12 intake from Desktop, local only)
参考图/
