# Python
__pycache__/
*.py[cod]
*$py.class
# ⚠️ 例外（2026-09-19 修复）：academic-toolkit/tools/ 下的 16 个 .pyc 是**受控分发件**，
# 与同名 .py 加载器配对（加载器由 tools/pyc_loader.py 生成，3.11 编译器锁定），
# 属本仓既有设计。上面 `*.py[cod]` 曾把它们全部排除 → 公开 clone 后 16 个工具
# 一律报 "xxx.pyc not found" 并 exit 1（CI 首次实测发现，属发布物残缺）。
!academic-toolkit/tools/*.pyc
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Virtual environments
.venv/
.venv*/
venv/
ENV/
env/
.env
.env.*
!.env.example
**/.env
**/.env.*
# 修复（2026-09-19）：上一行的 `**/.env.*` 会连带吃掉子目录的模板文件，
# 而 `!.env.example` 只对仓库根生效 → academic-toolkit/.env.example（纯 <REPLACE_ME> 模板，
# 无真实密钥）被误忽略，导致公开 clone 后 test_security_baseline 缺件失败。
!**/.env.example

# IDEs
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
Thumbs.db
Desktop.ini

# Testing
.pytest_cache/
.coverage
htmlcov/
.mypy_cache/
.ruff_cache/
.tox/

# Logs
*.log
logs/

# Internal development truth docs (not for public delivery)
dev-docs/

# Engine state and audit trails (not for public delivery)
.engine/
**/.engine/

# WorkBuddy 项目工作数据（AI 工作记忆，含本机绝对路径与个人身份线索；本仓为 PUBLIC，
# 故排除。2026-09-19 建立时首次出现 .workbuddy/memory/）
.workbuddy/

# Database
*.db
*.sqlite
*.sqlite3

# Node
node_modules/
.pnpm-store/

# Third-party competition material and private references (no redistribution right)
# 2026-09-23 v2.0 改造：原根级散置的 参考论文/ 参考图/ CUMCM论文模板/ 统一迁入
# assets-local/（英文子目录 award-papers / reference-figures / cumcm-templates），
# 整区一条规则隔离；子目录内容不改名（第三方原件保持原貌）。
assets-local/
参考论文使用指南.md
附件*.pdf
赛前试炼任务/
CUMCM2026Problems/
extracted_images/
screenshots/

# Real practice/validation workspaces
workspaces/

# ⚠️ 2026-09-19 用户裁定：公开基准集废弃入库（benchmarks/cumcm_public + six_domains_public）。
#   内容零丢失——77 文件/271KB 已归档至 dev-docs/archive/legacy-benchmarks-tests-20260919/（含恢复命令）。
#   随之废弃的唯一依赖测试：tests/test_cumcm_benchmark.py（另两个根级门禁测试已保留入库）。
benchmarks/

# ⚠️ 永久丢失留痕：benchmarks/cumcm_private/（私有基准）从未入库，仅存于磁盘；
#   2026-09-19 扫描确认该目录已随 benchmarks/ 整体从磁盘消失，git 历史中无任何对象可恢复。

# Legacy toolkit and release packages (kept locally for traceability)
releases/

# Browser automation state and acceptance-test scratch artifacts
.playwright-mcp/
acceptance_testing/

# Suite-internal private reference data and anti-AI baselines derived from award papers
# ⚠️ 2026-09-19 修正：原为整目录 `academic-toolkit/data/`。整目录规则有静默陷阱——
#    已 tracked 的老文件**免疫 ignore**（照常入库），但新增的交付件会**无声消失**
#    （git status 里根本不出现），导致本地全绿、公开 clone 缺件、对应测试全红。
#    故改为"忽略目录内容 + 显式放行交付件"。
#    ⚠️ negation 对被忽略的**目录**无效，必须写 `data/*` 而非 `data/`，否则 ! 规则全部失效。
#    （data/ 下 8 个历史文件已于 2026-08-28 入库，靠 tracked 免疫延续，不在此列）
academic-toolkit/data/*
!academic-toolkit/data/skill_tiers.json
!academic-toolkit/data/skill_routing_index.json
!academic-toolkit/data/skill_routing_critical.json
!academic-toolkit/data/contest_lessons.json
!academic-toolkit/data/contest_lessons.md
# 2026-09-20 基线加固轮三件门禁台账（lint 棘轮基线 / 密钥豁免 / 重复资产登记）
# ——CI 三道门禁步与对应测试直接消费，缺件即红
!academic-toolkit/data/lint_baseline.json
!academic-toolkit/data/secret_scan_allowlist.json
!academic-toolkit/data/duplicate_assets_registry.json
# ⚠️ 2026-09-22 修正（照上方 data/ 同款模板）：原为整目录规则 `academic-toolkit/baseline/`，
#    整目录规则有静默陷阱——已 tracked 的老文件**免疫 ignore**（照常入库），但新增的
#    交付件会**无声消失**（git status 里根本不出现），导致本地全绿、公开 clone 缺件。
#    故改为"忽略目录内容 + 显式放行交付件"。
#    ⚠️ negation 对被忽略的**目录**无效，必须写 `baseline/*` 而非 `baseline/`，否则 ! 规则全部失效。
academic-toolkit/baseline/*
!academic-toolkit/baseline/REFERENCE_PAPER_ANTI_AI_ANALYSIS.md
!academic-toolkit/baseline/human_paper_baseline.json

# Local test and vision-call residue
academic-toolkit/_test_input.md
**/.drawio_vision_calls.json
**/.tikz_vision_calls.json
# Upstream fork staging (local research only, not tracked)
vendor/

# ZCode host-local layer: skills junction + 会话产物（不入库；2026-09-23 v2.0 起整目录本地化）
.zcode/
方法武器库/

# Modex v3 技能包原包（本地对照源，已定向吸收，赛后评估入库方式）
modex-3-skills/

# 无 License 上游技能（本地完整复刻可用，不入公开仓——侵权红线隔离）
academic-toolkit/skills/plot-from-data/
academic-toolkit/skills/plot-from-image/
academic-toolkit/skills/visio-image-rebuilder/
academic-toolkit/skills/paper-framework-figure-studio-pro/

# No-license upstream skill batch (2026-09-11): eco-community-plots
academic-toolkit/skills/eco-community-plots/

# 竞赛模板集中字体库 + 模板内嵌字体（2026-09-22 P2 资产落位：103MB+~84MB 二进制不入公开仓；恢复源见 _templates/huawei/README.md）
academic-toolkit/skills/comp-paper-zh/_templates/_fonts-local/
academic-toolkit/skills/comp-paper-zh/_templates/**/*.[tT][tT][fF]
academic-toolkit/skills/comp-paper-zh/_templates/**/*.[tT][tT][cC]
academic-toolkit/skills/comp-paper-zh/_templates/**/*.[oO][tT][fF]
# 官方附件3 Word/PDF（第三方模板，无再分发授权，本机参照用）
academic-toolkit/skills/comp-paper-zh/_templates/huawei/official_docx/
